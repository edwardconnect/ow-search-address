import styled from 'styled-components';

const StyledContainer = styled.div`
  max-width: 600px;
  margin: 0 auto;
  padding-top: 30px;
  padding-bottom: 60px;
`;

export default {
  StyledContainer
};

