import styled from 'styled-components';

const StyledWrapper = styled.div`
  margin-bottom: 30px;
`;

const StyledLabel = styled.label`
  color: white;
  font-size: 14px;
  font-weight: 100;
  display: block;
  margin-bottom: 20px;
`;

export default {
  StyledWrapper,
  StyledLabel,
};
